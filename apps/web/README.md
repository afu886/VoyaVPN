# @voya/web

Placeholder package for the future VoyaVPN web administration app.

There is currently no HTTP API surface in VoyaVPN. The backend's only application boundary is Tauri IPC, so a web administration app must first introduce an explicit server-side API surface. Do not copy `src/ipc` into this package.
