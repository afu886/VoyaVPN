# @voya/mobile

Placeholder package for the future VoyaVPN bare React Native app.

This package must not consume `@voya/ui`; that package is expected to remain Radix and DOM specific. When React Native is introduced, configure Metro `watchFolders` and resolver settings deliberately so pnpm workspace symlinks resolve correctly.
